# Ejemplo de uso

Para utilizar este módulo y calcular la integral, primero debes asegurarte de tener instaladas las librerías `numpy`, `scipy` y `matplotlib`.

Puedes correr el script directamente desde tu terminal utilizando Python:

```bash
python cuadrature.py
```
